import LoginForm from "./LoginForm.tsx";
import "../styles.css";

const LoginPage = () => {
    return (
        <div className="login-page">
            <LoginForm/>
        </div>
    );
};

export default LoginPage;